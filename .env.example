# Ejemplo de variables de entorno
# Copia este archivo a `.env` y completa los valores antes de usar en producción

# Django
SECRET_KEY=django-insecure-coloca-aqui-tu-clave
DEBUG=True

# Base de datos (para MySQL ejemplo)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=
